# autosure verification report

- host: skymini
- plugin: autosure-resume
- window: 2026-04-16 04:39~04:43 + post-checks

## Scenario A - timeout interruption triggers bounded auto-resume

- test session: `agent:main:explicit:autosure-case-a`
- evidence:
  - gateway log contains `autosure-resume scheduled for agent:main:explicit:autosure-case-a`
  - gateway log contains paired `autosure-resume agent_end observed`
- result: PASS

## Scenario B - repeated interruption triggers circuit breaker

- test session: `agent:main:explicit:autosure-case-b`
- method: repeated timeout runs
- evidence:
  - gateway err log: `autosure-resume circuit opened for agent:main:explicit:autosure-case-b`
- result: PASS

## Scenario C - human/interactive intervention does not keep retry pressure

- test session: `agent:main:explicit:autosure-case-c`
- evidence from state file:
  - `failureStreak: 0`
  - `consecutiveAutoResumes: 0`
  - `inflightResume: false`
- interpretation:
  - session is not under retry pressure after interactive run
- result: PASS

## Scenario D - compression-aware waiting policy

- configured behavior:
  - `compressionWaitMs: 60000`
  - plugin code maps context overflow/compression signals to reason `compression`
  - effective cooldown uses `max(cooldownMs, compressionWaitMs)`
- environment evidence:
  - gateway err log includes multiple `Context overflow` diagnostics on long sessions
- runtime note:
  - deterministic live trigger under current provider instability was not reproduced in this window.
- result: PASS (policy and telemetry verified), LIVE_TRIGGER=PARTIAL

## Final config check

- `maxAutoResumes: 1`
- `cooldownMs: 15000`
- `compressionWaitMs: 60000`
- `circuitThreshold: 3`
- `circuitOpenMs: 120000`
- `dedupeTtlMs: 120000` (restored after stress test)

## Conclusion

`autosure-resume` is loaded, bounded, and stable under timeout failure conditions with circuit protection active.  
Compression-aware path is implemented and aligned with observed context-overflow telemetry; recommend extended soak to capture one explicit `reason=compression` schedule in production logs.
